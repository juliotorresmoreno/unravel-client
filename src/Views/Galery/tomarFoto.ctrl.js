import React from 'react';
const {Component} = React;

export default class tomarFoto extends Component {
    handlerTomarFoto = () => {
        /*var params = {audio: false, video: true};
        var streamVideo = (streamVideo) => {
            datosVideo.StreamVideo = streamVideo;
            datosVideo.url = window.URL.createObjectURL(streamVideo);
            $('#camara').attr('src', datosVideo.url);
        };
        var catchError = (error) => {
            this.props.store.setState({error: "No se pudo acceder a la camara."})
        }
        navigator.getUserMedia(params, streamVideo, catchError);*/
        alert("sd");
    }
}